# Airflow
AIRFLOW_UID=1000
AIRFLOW_IMAGE_NAME=demo-airflow:latest
AIRFLOW__CORE__FERNET_KEY=ZmDfcTF7_60GrrY167zsiPd67pEvs0aGOv2oasOM1Pg=

# PostgreSQL
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow

# MinIO (S3-compatible data lake)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=miniominio
MINIO_BUCKET=raw

# Data warehouse (inside the same postgres, different DB)
DW_DB=demo_dw
DW_USER=dw_user
DW_PASSWORD=dw_password

# Metabase admin (auto-created by metabase-init on first run)
MB_ADMIN_EMAIL=admin@demo.local
MB_ADMIN_PASSWORD=metabase123
