name: 'jsonplaceholder'
version: '1.0.0'
profile: 'demo_profile'

model-paths: ["models"]
macro-paths: ["macros"]

models:
  jsonplaceholder:
    bronze:
      +schema: bronze
      +materialized: table
    silver:
      +schema: silver
      +materialized: table
    gold:
      +schema: gold
      +materialized: table
